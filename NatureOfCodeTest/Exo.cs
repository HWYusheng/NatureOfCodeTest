using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography;
using System.Text;
using System.Threading.Tasks;
using static System.Windows.Forms.VisualStyles.VisualStyleElement.TaskbarClock;

namespace NatureOfCodeTest
{
    internal class Exo
    {
        private float exp_offset(List<Body> hdr, int time_unit, float exp)
        {
            //Returns exposure offset (in days) of more than 0 if headers reveals the time was estimated at the start of the exposure rather than the middle
            if (true)
            {
                
                return exp = 0.0f;
            }
            return exp = 0.0f;
        }
        private float air_mass( float ra, float dec, float lat, float elevation, DateTime time)
        {
            float airmass = 0f;
            if (true)
            {

            }
            return airmass;
        }
    }
}
